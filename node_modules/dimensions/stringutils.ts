class StringUtils {
    static format(raw: string, ...args): string {
        return raw.replace(/{(\d+)}/g, function(match, number) { 
        return typeof args[number] != 'undefined'
            ? args[number] 
            : match
        ;
        });
    }
}

export default StringUtils;